# Custom Rules for Antigravity

## Sync Mandate
- Always keep the local Mac repository, the Cloudtop environment (`nitinagga.c.googlers.com`), and the remote GitHub repository in perfect synchronization.
- When any file is created, modified, or deleted locally on the Mac or remotely on the Cloudtop, ensure the changes are mirrored to the other environment.
- When committing changes, make sure they are pushed to the GitHub repository so that all three environments (Mac, Cloudtop, Git) remain aligned.
- Never commit secrets, credentials, or keys (e.g. `gcp-key.json`, `.env` files). Always verify `.gitignore` before syncing or committing.

---

## Registered Global Skills & Audio Architecture

### `deepmind-emotional-audio-engine`
* **Trigger**: Activated whenever creating, modifying, or generating voice briefings, executive audio storytelling, podcast co-hosting, or real-time voice synthesis.
* **Capabilities**:
  1. **Google DeepMind Neural Synthesis**: Uses `gemini-2.5-flash-preview-tts` and `gemini-3.1-flash-tts-preview` with prebuilt voice models (`Charon`, `Aoede`, `Puck`, `Kore`, `Fenrir`).
  2. **4,000+ Procedural Voice Matrix**: 5 Neural Timbres $\times$ 25 Global Accents $\times$ 8 C-Suite Archetypes $\times$ 4 Age/Pacing Tiers.
  3. **"Prompt-to-Voice" Custom AI Voice Designer**: Real-time compilation of natural language persona descriptions into acoustic character models.
  4. **3-Stage Spectral Denoising & 5-Band Formant Convolver**: In-browser mic recording and MP3 cloning with background noise gating and $F1/F2/F3/F4/F5$ vocal tract filtering.
  5. **Live Web Audio DSP Broadcast Rack**: $+2.5\text{dB}$ $180\text{Hz}$ low-shelf warmth EQ, broadcast compression, and Act-specific ASMR/triumph dynamic morphing.
  6. **Enterprise Security**: 100% Google Cloud native, zero third-party data egress, SHA-256 disk and memory caching for 0ms replays.

---

### `deepmind-video-generation`
* **Trigger**: Activated whenever generating, scripting, rendering, or animating executive video briefings, cinematic architecture fly-throughs, talking avatar narration, or HTML5 Canvas / Remotion slide video rendering.
* **Capabilities**:
  1. **Google DeepMind Veo 2 / Imagen 3 Video**: Generates cinematic enterprise b-roll, isometric cloud datacenter fly-throughs, and particle pipeline motion.
  2. **5-Act Storyboard Compiler**: Pacing decomposition (Act 1: Executive Hook $\rightarrow$ Act 2: Diagnostic Reality $\rightarrow$ Act 3: Target Architecture $\rightarrow$ Act 4: Transformation Horizon $\rightarrow$ Act 5: Financial Realization).
  3. **60fps Canvas / Remotion Video Engine**: In-browser GPU-accelerated motion graphics with animated radar sweeps, live metric counters, and risk heatmap pulses.
  4. **Zero Third-Party Egress**: 100% Google Cloud native with strict digital watermarking and board governance classification.

---

### `universal-document-cloud-hub`
* **Trigger**: Activated whenever exporting, previewing, transforming, or integrating deliverables across Google Slides/PPTX, Google Sheets/XLSX, Google Docs/Word, PDF, Flat CSV, and Draw.io architecture XMLs.
* **Capabilities**:
  1. **Gmail / Google Drive Attachment Preview**: Fullscreen glassmorphic header with format badges, tabbed doc switching, and live interactive data grids.
  2. **1-Click Cloud Launch Protocol**: Zero-download direct creation and editing in Google Slides (`slides.new`), Google Sheets (`sheets.new`), Google Docs (`docs.new`), and Draw.io (`app.diagrams.net`).
  3. **In-Browser Multi-Format Renderers**: 16:9 Slide Deck Carousel, multi-tab Excel spreadsheet grid with real-time search, executive memorandum formatter, and vector SVG architecture pan/zoom.
  4. **Explicit Geometry & Multi-Sheet Exporters**: PptxGenJS column layout safety (`colW`), ExcelJS multi-sheet workbooks, WordprocessingML `.docx` briefs, and 1-click All Deliverables Bundle packaging.

